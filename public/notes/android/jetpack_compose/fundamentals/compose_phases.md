# Compose phases

Jetpack compose phases are

1. Data
2. Composition = Finds out what to draw, creates a UI tree (Layout nodes) which other the next phase will use.
3. layout = Calculates the position of the various design elements.
4. Drawing = Lays out the various design elements.
5. UI.